# realchat
Transmission is a real-time chat app built with FastAPI and WebSockets, featuring user accounts (JWT auth), multiple chat rooms, and persistent message history via SQLite — all packed into a single-file backend with a clean blue-and-white frontend.
